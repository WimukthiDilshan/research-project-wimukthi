# Teacher Dashboard

Simple React dashboard for lesson-based explainability.

## Run

```powershell
npm install
npm run dev
```

The Vite dev server proxies `/api` requests to `http://127.0.0.1:8000`.
